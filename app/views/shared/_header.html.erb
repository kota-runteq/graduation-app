<header>
  <nav class='navbar navbar-dark bg-info'>
    <div class='container-fluid d-flex justify-content-between'>
      <a class='navbar-brand fw-bold' href='/'>
        ChainMenu+PFC
      </a>
      <div class='dropdown'>
        <button class='navbar-toggler border-0 shadow-none' type='button' data-bs-toggle='dropdown' aria-expanded='false' style="background-color: #222;">
          <span class='navbar-toggler-icon'></span>
        </button>
        <ul class='dropdown-menu dropdown-menu-end'>
          <li>
            <%= link_to 'トップページ', '/', class: 'dropdown-item' %>
          </li>
          <% if user_signed_in? %>
            <li>
            <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "dropdown-item btn btn-link" %>
            </li>
          <% else %>
            <li>
              <%= link_to 'ログイン', new_user_session_path, class: 'dropdown-item' %>
            </li>
            <li>
              <%= link_to '新規登録', new_user_registration_path,  class: 'dropdown-item' %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  </nav>
</header>